[package]
name = "oop_project"
version = "0.1.0"
edition = "2024"

[dependencies]
mac_address = "1.1.8"
sqlx = { version = "0.8.6", features = ["sqlite"] }
tokio = { version = "1.50.0", features = ["macros", "net", "rt-multi-thread"] }
